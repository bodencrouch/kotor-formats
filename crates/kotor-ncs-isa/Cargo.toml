[package]
name = "kotor-ncs-isa"
description = "Instruction set definitions for KotOR/Odyssey NCS compiled script bytecode."
version = "0.2.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

# Data only: no dependencies, so a compiler backend can take this without
# inheriting a reader, and a disassembler can take it without inheriting a
# compiler.
[dependencies]
